<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.dao.AdminDao">
    
    <select id="findByUsername" resultType="com.boot.dto.Admin">
        SELECT id, username, password, name, email, is_active as isActive, 
               created_at as createdAt, updated_at as updatedAt
        FROM admin
        WHERE username = #{username}
        AND is_active = 1
    </select>
    
    <select id="findByUsernameAndPassword" resultType="com.boot.dto.Admin">
        SELECT id, username, password, name, email, is_active as isActive, 
               created_at as createdAt, updated_at as updatedAt
        FROM admin
        WHERE username = #{username}
        AND password = #{password}
        AND is_active = 1
    </select>
    
</mapper>